#include "main.h"
int _isdigit(int c)
{
        (void)c;
        return (c);
}

int _strlen(char *s)
{
        (void)s;
        return (0);
}

void _puts(char *s)
{
        (void)s;
}

char *_strcpy(char *dest, char *src)
{
        (void)dest;
        (void)src;
        return (dest);
}

int _atoi(char *s)
{
        (void)s;
        return (0);
}
