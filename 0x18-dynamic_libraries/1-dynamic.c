#include "main.h"
int _putchar(char c)
{
        (void)c;
        return (0);
}

int _islower(int n)
{
        (void)n;
        return (n);
}

int _isalpha(int n)
{
        (void)n;
        return (n);
}

int _abs(int n)
{
        (void)n;
        return (n);
}

int _isupper(int n)
{
        (void)n;
        return (n);
}
